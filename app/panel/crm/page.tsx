import Link from "next/link";
import {getPanelContext} from "@/lib/panel-context";
import {PanelDrawer} from "../components/panel-drawer";
import {archiveOpportunity,moveOpportunity,updateOpportunity} from "./actions";
import {RequestEntryForm} from "./request-entry-form";
import {ProposalBuilderForm} from "./proposal-builder-form";
import {requestStageNames,requestStages} from "./request-status";
import "./crm.css";import "./request-page.css";

type SearchParams=Promise<{arama?:string;durum?:string;temsilci?:string}>;type Details={service_type?:string;academic_level?:string;university?:string;department?:string;language?:string;scope?:string};type Opportunity={id:string;title:string;customer_name:string;contact_email:string|null;contact_phone:string|null;stage:string;estimated_value:number;expected_close_date:string|null;source:string|null;notes:string|null;request_details:Details|null;assigned_employee_id:string|null};type SalesRepresentative={id:string;full_name:string;job_title:string|null};
const clean=(v?:string)=>(v??"").trim().slice(0,100);const active=new Set(["lead","qualified"]);
export default async function RequestsPage({searchParams}:{searchParams:SearchParams}){const {arama,durum,temsilci}=await searchParams;const search=clean(arama).toLocaleLowerCase("tr-TR");const selected=clean(durum);const selectedRepresentative=clean(temsilci);const {supabase,membership,modules}=await getPanelContext();if(!modules.some(m=>m.code==="crm"))throw new Error("CRM modülüne erişiminiz yok.");const [{data,error},{data:stages,error:stageError},{data:employeeData,error:employeeError}]=await Promise.all([supabase.from("crm_opportunities").select("id,title,customer_name,contact_email,contact_phone,stage,estimated_value,expected_close_date,source,notes,request_details,assigned_employee_id").eq("organization_id",membership.organization_id).order("updated_at",{ascending:false}),supabase.from("organization_crm_stages").select("code").eq("organization_id",membership.organization_id).eq("is_active",true),supabase.from("hr_employees").select("id,full_name,job_title").eq("organization_id",membership.organization_id).eq("employment_status","active").eq("can_receive_sales_requests",true).order("full_name")]);if(error)throw new Error("Talepler okunamadı: "+error.message);if(stageError)throw new Error("Talep ayarları okunamadı: "+stageError.message);if(employeeError)throw new Error("Satış temsilcileri okunamadı: "+employeeError.message);const representatives=(employeeData??[]) as SalesRepresentative[];const representativeMap=new Map(representatives.map((item)=>[item.id,item.full_name]));const academicMode=(stages??[]).some(s=>s.code==="academic_review");const all=(data??[]) as Opportunity[];const rows=all.filter(i=>{const hay=[i.customer_name,i.title,i.contact_email,i.contact_phone,i.request_details?.service_type,representativeMap.get(i.assigned_employee_id??"")].filter(Boolean).join(" ").toLocaleLowerCase("tr-TR");const representativeMatches=!selectedRepresentative||(selectedRepresentative==="atanmamis"?!i.assigned_employee_id:i.assigned_employee_id===selectedRepresentative);return(!search||hay.includes(search))&&(selected==="tumu"?true:selected?i.stage===selected:active.has(i.stage))&&representativeMatches});const counts=(code:string)=>all.filter(i=>i.stage===code).length;
return <div className="crm-page-stack"><div className="panel-pagehead"><div><small className="panel-kicker">TALEP YÖNETİMİ</small><h1>Talepler</h1><p>Yeni talepleri inceleyin, satış temsilcisine atayın ve teklif aşamasına devredin.</p></div><div className="panel-page-actions"><span className="status-pill">{rows.length} kayıt</span><PanelDrawer triggerLabel="+ Yeni talep" title={academicMode?"Talep Girişi":"Yeni talep"} description="Müşteri ve talep bilgilerini kaydedin."><RequestEntryForm academicMode={academicMode} salesRepresentatives={representatives}/></PanelDrawer></div></div><section className="crm-metrics"><article><small>YENİ TALEP</small><strong>{counts("lead")}</strong><span>İlk değerlendirme</span></article><article><small>TALEP İNCELENİYOR</small><strong>{counts("qualified")}</strong><span>İnceleme sürecinde</span></article><article><small>TEKLİFLERE DEVREDİLDİ</small><strong>{counts("proposal")}</strong><span>Teklifler bölümünde</span></article><article><small>ARŞİVLENDİ</small><strong>{counts("lost")}</strong><span>İptal edilen kayıtlar</span></article></section><section className="panel-card crm-filter-card"><form action="/panel/crm" className="crm-filter-form"><label><span>Müşteri / talep ara</span><input name="arama" defaultValue={arama??""}/></label><label><span>Durum</span><select name="durum" defaultValue={selected}><option value="">Aktif Talepler</option><option value="tumu">Tüm Kayıtlar</option>{requestStages.map(s=><option value={s.code} key={s.code}>{s.name}</option>)}</select></label><label><span>Satış temsilcisi</span><select name="temsilci" defaultValue={selectedRepresentative}><option value="">Tüm temsilciler</option><option value="atanmamis">Atanmamış</option>{representatives.map((employee)=><option value={employee.id} key={employee.id}>{employee.full_name}</option>)}</select></label><div><button className="panel-primary">Filtrele</button><a className="panel-secondary" href="/panel/crm">Temizle</a></div></form></section><section className="crm-record-list">{rows.map(item=>{const d=item.request_details??{};const ownerName=item.assigned_employee_id?representativeMap.get(item.assigned_employee_id)??"Pasif personel":"Atanmamış";const preview=<div className="crm-request-preview"><span>{requestStageNames[item.stage]??item.stage}</span><h3>{item.customer_name}</h3><h4>{item.title}</h4><dl><div><dt>Hizmet</dt><dd>{d.service_type||"Belirtilmedi"}</dd></div><div><dt>Satış temsilcisi</dt><dd>{ownerName}</dd></div><div><dt>Telefon</dt><dd>{item.contact_phone||"Belirtilmedi"}</dd></div><div><dt>E-posta</dt><dd>{item.contact_email||"Belirtilmedi"}</dd></div><div><dt>Üniversite</dt><dd>{d.university||"Belirtilmedi"}</dd></div><div><dt>Teslim</dt><dd>{item.expected_close_date?new Date(item.expected_close_date+"T00:00:00").toLocaleDateString("tr-TR"):"Belirtilmedi"}</dd></div></dl>{d.scope?<p>{d.scope}</p>:null}{item.notes?<p>{item.notes}</p>:null}</div>;const edit=<form className="panel-form" action={updateOpportunity}><input type="hidden" name="opportunity_id" value={item.id}/><input type="hidden" name="current_details" value={JSON.stringify(d)}/><label>Talep konusu<input name="title" required defaultValue={item.title}/></label><label>Müşteri / kurum<input name="customer_name" required defaultValue={item.customer_name}/></label><label>Hizmet türü<input name="service_type" defaultValue={d.service_type||""}/></label><label>Satış temsilcisi<select name="assigned_employee_id" defaultValue={item.assigned_employee_id??""}><option value="">Atanmamış</option>{representatives.map((employee)=><option value={employee.id} key={employee.id}>{employee.full_name}</option>)}</select></label><label>Telefon<input name="contact_phone" defaultValue={item.contact_phone||""}/></label><label>E-posta<input name="contact_email" defaultValue={item.contact_email||""}/></label><label className="wide">Kapsam<textarea name="scope" defaultValue={d.scope||""}/></label><div className="wide panel-form-actions"><button className="panel-primary">Kaydet</button></div></form>;return <article className="panel-card crm-record crm-request-card" key={item.id}><div className="crm-record-main"><div className="crm-record-heading"><span className="crm-record-number">TLP-{item.id.slice(0,8).toUpperCase()}</span><span className="status-pill">{requestStageNames[item.stage]??item.stage}</span></div><h2>{item.customer_name}</h2><h3>{item.title}</h3><div className="crm-record-meta"><span>{item.contact_phone||"Telefon yok"}</span><span>{item.contact_email||"E-posta yok"}</span><span>Temsilci: {ownerName}</span><span>{item.source||"Kaynak yok"}</span></div>{d.service_type?<p><b>{d.service_type}</b></p>:null}{d.scope?<p>{d.scope}</p>:null}</div><aside className="crm-request-actions"><small>TALEP İŞLEMLERİ</small>{item.stage==="proposal"?<Link className="panel-primary" href="/panel/crm/proposals">Tekliflere Git</Link>:<PanelDrawer triggerLabel="Teklif Oluştur" title="Teklif Oluştur"><ProposalBuilderForm opportunityId={item.id} customerName={item.customer_name} title={item.title} scope={d.scope||item.notes||item.title}/></PanelDrawer>}<PanelDrawer triggerLabel="Önizle" title="Talep Önizleme">{preview}</PanelDrawer><PanelDrawer triggerLabel="Düzenle" title="Talebi Düzenle">{edit}</PanelDrawer><form action={archiveOpportunity}><input type="hidden" name="opportunity_id" value={item.id}/><input type="hidden" name="archive_reason" value="Talep arşivlendi."/><button className="panel-danger">Sil / Arşivle</button></form><form className="crm-status-form" action={moveOpportunity}><input type="hidden" name="opportunity_id" value={item.id}/><label>Durum<select name="stage" defaultValue={item.stage}>{requestStages.map(s=><option value={s.code} key={s.code}>{s.name}</option>)}</select></label><input name="lost_reason" placeholder="Arşiv nedeni"/><button className="panel-secondary">Durumu Güncelle</button></form></aside></article>})}{!rows.length?<section className="panel-card crm-empty-state"><h2>Eşleşen talep bulunamadı</h2></section>:null}</section></div>}
